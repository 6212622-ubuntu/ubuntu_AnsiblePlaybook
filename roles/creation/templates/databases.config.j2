version: '3.8'

services:
  postgres:
    image: postgres:15
    restart: always
    env_file: ../.env
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - {{swarm_app_database_volume}}:/var/lib/postgresql/data
